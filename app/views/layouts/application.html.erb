<!DOCTYPE html>
<html>
  <head>
    <title>DevKitchen</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>

  </head>

  <body>
    <nav class="navbar navbar-fixed-top navbar-expand-md navbar-dark bg-primary">
        <div class="d-flex">
            <a class="navbar-brand" <%= link_to "DEV KITCHEN", categories_path %></a>
        </div>
        <button class="navbar-toggler order-3" type="button" data-toggle="collapse" data-target="#collapsingNavbar">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="navbar-collapse collapse" id="collapsingNavbar">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="#">Menu <span class="sr-only">Home</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="//codeply.com">Share</a>
                </li>
                <li class="nav-item">
                    <%= form_tag(recipes_path, :method => "get", id: "search-form") do %>
                                          <%= text_field_tag :search, params[:search], placeholder: "Search Recipes" %>
                                          <%= submit_tag "Search", :name => nil %>
                                        <% end %>
                </li>
            </ul>
        </div>
        <a class="navbar-text small text-truncate mt-1" href="#">Log In</a>
    </nav>

    <% flash.each do |message_type, message| %>
      <div><%= message %></div>
    <% end %>

    <%= yield %>
  </body>
</html>
