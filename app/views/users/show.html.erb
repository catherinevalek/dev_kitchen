<div class="container">
  <h1><%=@user.username.capitalize%>'s Profile</h1>
  <ul>
  <% @user.recipes.each do |recipe| %></p>
    <li><%= link_to recipe.name, recipe %></li>
  <% end %>
  </ul>
</div>
